# Optional fallback market-data provider.
# Yahoo Finance remains the default provider when this is unset.
TWELVE_DATA_API_KEY=

# Optional AI provider. If these are unset, the assistant uses its safe local fallback.
AI_API_KEY=
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=
