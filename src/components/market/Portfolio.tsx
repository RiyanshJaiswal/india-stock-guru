import { useEffect, useMemo, useState, type FormEvent } from "react";
import { Link } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { Pencil, Plus, Trash2, X } from "lucide-react";

import type { Position } from "@/data/market";
import { inr } from "@/data/market";
import { quotesQuery, searchQuery } from "@/lib/market-queries";
import { num, signed, stripSuffix } from "@/lib/market-types";
import { Delta } from "./Delta";
import { StatTile } from "./StatTile";
import { cn } from "@/lib/utils";

const STORAGE_KEY = "dalal-desk.portfolio.v1";
const MAX_DASHBOARD_ROWS = 6;
type StockSuggestion = { symbol: string; name: string };
const STOCK_SUGGESTIONS: StockSuggestion[] = [
  { symbol: "RELIANCE.NS", name: "Reliance Industries" }, { symbol: "TCS.NS", name: "Tata Consultancy Services" },
  { symbol: "INFY.NS", name: "Infosys" }, { symbol: "HDFCBANK.NS", name: "HDFC Bank" },
  { symbol: "ICICIBANK.NS", name: "ICICI Bank" }, { symbol: "SBIN.NS", name: "State Bank of India" },
  { symbol: "BHARTIARTL.NS", name: "Bharti Airtel" }, { symbol: "ITC.NS", name: "ITC" },
  { symbol: "LT.NS", name: "Larsen & Toubro" }, { symbol: "AXISBANK.NS", name: "Axis Bank" },
  { symbol: "KOTAKBANK.NS", name: "Kotak Mahindra Bank" }, { symbol: "HINDUNILVR.NS", name: "Hindustan Unilever" },
  { symbol: "BAJFINANCE.NS", name: "Bajaj Finance" }, { symbol: "MARUTI.NS", name: "Maruti Suzuki India" },
  { symbol: "TATAMOTORS.NS", name: "Tata Motors" }, { symbol: "TATASTEEL.NS", name: "Tata Steel" },
  { symbol: "SUNPHARMA.NS", name: "Sun Pharmaceutical" }, { symbol: "ADANIENT.NS", name: "Adani Enterprises" },
  { symbol: "NTPC.NS", name: "NTPC" }, { symbol: "POWERGRID.NS", name: "Power Grid Corporation" },
  { symbol: "HCLTECH.NS", name: "HCL Technologies" }, { symbol: "WIPRO.NS", name: "Wipro" },
  { symbol: "TITAN.NS", name: "Titan Company" }, { symbol: "NESTLEIND.NS", name: "Nestle India" },
  { symbol: "CIPLA.NS", name: "Cipla" }, { symbol: "HAL.NS", name: "Hindustan Aeronautics" },
  { symbol: "IRCTC.NS", name: "IRCTC" }, { symbol: "TRENT.NS", name: "Trent" },
  { symbol: "DMART.NS", name: "Avenue Supermarts (DMart)" }, { symbol: "GAIL.NS", name: "GAIL India" },
];
function normalizeSymbol(value: string) { const raw = value.trim().toUpperCase().replace(/^NSE:/, ""); if (!raw) return ""; return raw.endsWith(".NS") || raw.endsWith(".BO") ? raw : `${raw}.NS`; }
function readPositions(): Position[] { if (typeof window === "undefined") return []; try { const parsed = JSON.parse(window.localStorage.getItem(STORAGE_KEY) ?? "[]"); return Array.isArray(parsed) ? parsed.filter((p): p is Position => typeof p?.symbol === "string" && typeof p?.quantity === "number" && p.quantity > 0 && typeof p?.avgPrice === "number" && p.avgPrice > 0) : []; } catch { return []; } }
function savePositions(value: Position[]) { window.localStorage.setItem(STORAGE_KEY, JSON.stringify(value)); }

export function Portfolio() {
  const [positions, setPositions] = useState<Position[]>([]); const [ready, setReady] = useState(false);
  const [showAdd, setShowAdd] = useState(false); const [editingSymbol, setEditingSymbol] = useState<string | null>(null);
  const [symbol, setSymbol] = useState(""); const [suggestionSelected, setSuggestionSelected] = useState(false);
  const [quantity, setQuantity] = useState(""); const [avgPrice, setAvgPrice] = useState(""); const [formError, setFormError] = useState("");
  useEffect(() => { setPositions(readPositions()); setReady(true); }, []);
  const symbols = useMemo(() => positions.map((p) => p.symbol), [positions]);
  const { data, isFetching } = useQuery({ ...quotesQuery(symbols), placeholderData: (previousData) => previousData });
  const { data: remoteSuggestions } = useQuery(searchQuery(symbol));
  const rows = positions.map((position) => { const quote = data?.find((q) => q.symbol.trim().toUpperCase() === position.symbol.trim().toUpperCase()); const ltp = quote?.price ?? null; const invested = position.avgPrice * position.quantity; const current = ltp === null ? null : ltp * position.quantity; const local = STOCK_SUGGESTIONS.find((s) => s.symbol.toUpperCase() === position.symbol.toUpperCase()); return { ...position, ltp, name: quote?.name ?? local?.name ?? "—", invested, current, gain: current === null ? null : current - invested, gainPct: ltp === null ? null : ((ltp - position.avgPrice) / position.avgPrice) * 100 }; });
  const invested = rows.reduce((sum, r) => sum + r.invested, 0); const priced = rows.length > 0 && rows.every((r) => r.current !== null); const current = priced ? rows.reduce((sum, r) => sum + (r.current ?? 0), 0) : null; const pnl = current === null ? null : current - invested; const pnlPercent = pnl === null || invested === 0 ? null : (pnl / invested) * 100;
  const suggestions = useMemo(() => { const q = symbol.trim().toLowerCase(); if (!q || suggestionSelected) return []; const local = STOCK_SUGGESTIONS.filter((s) => s.name.toLowerCase().includes(q) || s.symbol.toLowerCase().replace(/\.ns$|\.bo$/i, "").includes(q)); const remote = (remoteSuggestions ?? []).map((s) => ({ symbol: s.symbol, name: s.name })); return [...remote, ...local].filter((s, i, list) => list.findIndex((x) => x.symbol.toUpperCase() === s.symbol.toUpperCase()) === i).slice(0, 6); }, [symbol, suggestionSelected, remoteSuggestions]);
  const persist = (next: Position[]) => { setPositions(next); savePositions(next); };
  const resetForm = () => { setSymbol(""); setSuggestionSelected(false); setQuantity(""); setAvgPrice(""); setEditingSymbol(null); setFormError(""); };
  const openAdd = () => { resetForm(); setShowAdd(true); };
  const openEdit = (p: Position) => { setSymbol(stripSuffix(p.symbol)); setSuggestionSelected(true); setQuantity(String(p.quantity)); setAvgPrice(String(p.avgPrice)); setEditingSymbol(p.symbol); setShowAdd(true); };
  const selectSuggestion = (s: StockSuggestion) => { setSymbol(s.symbol.replace(/\.NS$/i, "")); setSuggestionSelected(true); setFormError(""); };
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const normalized = normalizeSymbol(symbol); const qty = Number(quantity); const price = Number(avgPrice); if (!normalized || !Number.isFinite(qty) || qty <= 0 || !Number.isFinite(price) || price <= 0) { setFormError("Stock symbol, quantity aur buy price valid enter karo."); return; } if (editingSymbol) persist(positions.map((p) => p.symbol === editingSymbol ? { symbol: normalized, quantity: qty, avgPrice: price } : p)); else { const existing = positions.find((p) => p.symbol === normalized); if (existing) { const totalQty = existing.quantity + qty; const weightedAvg = (existing.quantity * existing.avgPrice + qty * price) / totalQty; persist(positions.map((p) => p.symbol === normalized ? { ...p, quantity: totalQty, avgPrice: Number(weightedAvg.toFixed(4)) } : p)); } else persist([...positions, { symbol: normalized, quantity: qty, avgPrice: price }]); } setShowAdd(false); resetForm(); };
  const remove = (s: string) => persist(positions.filter((p) => p.symbol !== s));
  return <section className="panel p-4 sm:p-5" aria-label="Portfolio">
    <header className="grid grid-cols-[minmax(0,1fr)_auto] items-center gap-3"><div className="min-w-0"><h2 className="truncate text-sm font-bold tracking-widest uppercase">Portfolio</h2><p className="mt-1 text-xs text-muted-foreground">Your holdings · saved on this device</p></div><div className="flex items-center gap-2">{ready && positions.length > 0 && <Delta className="shrink-0" changePercent={pnlPercent} />}<button type="button" onClick={openAdd} className="inline-flex cursor-pointer items-center gap-1.5 rounded-lg border border-primary/40 bg-primary/10 px-3 py-2 text-xs font-semibold text-primary hover:bg-primary/15"><Plus className="h-3.5 w-3.5" />Add holding</button></div></header>
    <div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3"><StatTile label="Current value" value={current === null ? "—" : inr(current, 0)} /><StatTile label="Invested" value={inr(invested, 0)} /><StatTile label="Overall P&L" value={pnl === null ? "—" : `${pnl >= 0 ? "+" : "−"}${inr(Math.abs(pnl), 0)}`} tone={pnl === null ? undefined : pnl >= 0 ? "bull" : "bear"} /></div>
    {!ready ? <div className="mt-5 rounded-xl bg-surface-2/50 px-4 py-8 text-center text-sm text-muted-foreground">Loading portfolio…</div> : positions.length === 0 ? <div className="mt-5 rounded-xl border border-dashed border-border bg-surface-2/30 px-4 py-8 text-center"><p className="text-sm font-semibold">No holdings added yet</p><p className="mt-1 text-xs text-muted-foreground">Add the stocks you actually own to track quantity, average buy price and live P&L.</p><button type="button" onClick={openAdd} className="mt-4 inline-flex items-center gap-1.5 rounded-lg bg-primary px-4 py-2 text-xs font-bold text-primary-foreground"><Plus className="h-3.5 w-3.5" />Add your first holding</button></div> : <><div className="mt-4 -mx-1 overflow-x-auto"><table className="w-full min-w-[38rem] border-separate border-spacing-y-1 px-1 text-sm"><thead><tr className="text-left text-[11px] tracking-wider text-muted-foreground uppercase"><th className="font-medium">Stock</th><th className="text-right font-medium">Qty</th><th className="text-right font-medium">Avg</th><th className="text-right font-medium">LTP</th><th className="text-right font-medium">P&L</th><th className="w-16 text-right font-medium">Action</th></tr></thead><tbody>{rows.slice(0, MAX_DASHBOARD_ROWS).map((row) => <tr key={row.symbol} className="bg-surface-2/50"><td className="rounded-l-lg px-3 py-2.5"><p className="font-semibold">{stripSuffix(row.symbol)}</p><p className="truncate text-xs text-muted-foreground">{row.name}</p></td><td className="num px-2 text-right">{row.quantity}</td><td className="num px-2 text-right text-muted-foreground">{num(row.avgPrice)}</td><td className="num px-2 text-right">{row.ltp === null && isFetching ? "…" : num(row.ltp)}</td><td className={cn("num px-2 text-right font-semibold", row.gain === null ? "text-muted-foreground" : row.gain >= 0 ? "text-bull" : "text-bear")}>{row.gain === null && isFetching ? "…" : signed(row.gain, 0)}<span className="block text-[11px] font-medium opacity-80">{row.gainPct === null ? "" : `${signed(row.gainPct)}%`}</span></td><td className="rounded-r-lg px-2 text-right"><button type="button" onClick={() => openEdit(row)} className="inline-flex cursor-pointer items-center justify-center rounded-lg p-2 text-muted-foreground hover:bg-surface-2 hover:text-foreground" aria-label={`Edit ${stripSuffix(row.symbol)}`}><Pencil className="h-3.5 w-3.5" /></button></td></tr>)}</tbody></table></div>{rows.length > MAX_DASHBOARD_ROWS && <Link to="/portfolio" className="mt-3 flex w-full items-center justify-center rounded-lg border border-border bg-surface-2/40 py-2 text-xs font-semibold text-muted-foreground hover:text-foreground">View all holdings · {rows.length}</Link>}</>}
    {showAdd && <div className="fixed inset-0 z-[80] flex items-center justify-center overflow-hidden bg-black/60 p-4 backdrop-blur-sm" role="dialog" aria-modal="true"><form onSubmit={submit} className="w-full max-w-md overflow-visible rounded-2xl border border-border bg-background p-5 shadow-2xl"><div className="flex items-center justify-between gap-3"><div><h3 className="text-base font-bold">{editingSymbol ? "Edit holding" : "Add holding"}</h3><p className="mt-1 text-xs text-muted-foreground">Enter what you actually bought.</p></div><button type="button" onClick={() => { setShowAdd(false); resetForm(); }} className="rounded-lg p-2 text-muted-foreground hover:bg-surface-2" aria-label="Close"><X className="h-4 w-4" /></button></div><div className="mt-5 grid gap-4"><label className="grid gap-1.5 text-xs font-semibold">Stock symbol<div className="relative"><input value={symbol} onChange={(e) => { setSymbol(e.target.value); setSuggestionSelected(false); setFormError(""); }} placeholder="Search company or symbol…" autoFocus autoComplete="off" className="w-full rounded-lg border border-border bg-surface-2 px-3 py-2.5 text-sm outline-none focus:border-primary" />{!suggestionSelected && symbol.trim() && suggestions.length > 0 && <div className="absolute left-0 top-[calc(100%+6px)] z-20 w-[max(100%,24rem)] max-w-[calc(100vw-2rem)] overflow-hidden rounded-xl border border-border bg-background shadow-xl"><div className="max-h-60 overflow-y-auto py-1">{suggestions.map((s) => <button key={s.symbol} type="button" onMouseDown={(e) => e.preventDefault()} onClick={() => selectSuggestion(s)} className="flex w-full items-center justify-between gap-3 px-3 py-2.5 text-left hover:bg-surface-2"><span className="min-w-0"><span className="block truncate text-sm font-semibold">{s.name}</span><span className="block text-[11px] text-muted-foreground">{s.symbol.replace(/\.NS$/i, "")} · NSE</span></span><span className="shrink-0 text-[10px] text-muted-foreground">Select</span></button>)}</div></div>}</div><span className="font-normal text-muted-foreground">Company name ya symbol ka kuch part type karo, suggestion select karo.</span></label><div className="grid grid-cols-2 gap-3"><label className="grid gap-1.5 text-xs font-semibold">Quantity<input type="number" min="0.0001" step="any" value={quantity} onChange={(e) => setQuantity(e.target.value)} className="rounded-lg border border-border bg-surface-2 px-3 py-2.5 text-sm" /></label><label className="grid gap-1.5 text-xs font-semibold">Buy price / share<input type="number" min="0.01" step="0.01" value={avgPrice} onChange={(e) => setAvgPrice(e.target.value)} className="rounded-lg border border-border bg-surface-2 px-3 py-2.5 text-sm" /></label></div>{formError && <p className="text-xs font-medium text-bear">{formError}</p>}</div><div className="mt-5 flex justify-end gap-2"><button type="button" onClick={() => { setShowAdd(false); resetForm(); }} className="rounded-lg border border-border px-4 py-2 text-xs font-semibold text-muted-foreground">Cancel</button><button type="submit" className="rounded-lg bg-primary px-4 py-2 text-xs font-bold text-primary-foreground">{editingSymbol ? "Save changes" : "Add holding"}</button></div></form></div>}
  </section>;
}
